spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: pass
#      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
